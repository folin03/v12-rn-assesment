import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  centre: {
    alignItems: 'center',
    marginHorizontal: 20,
  },

  normalText: {
    fontWeight: 'bold',
    fontSize: 20,
    color: 'black',
    marginTop: 20,
  },

  retailerPicture: {
    backgroundColor: 'lightblue',
    height: 200,
  },

  flatListContainer: {
    width: '100%',
  },

  listFooter: {
    height: 150,
  },
});
